#include "List.h"

Contact::Contact(string name, int number)
{
	string name = "";
	int number = 0;
};



Node::Node(Node* next, Contact name)//The Node will have a constructor that makes a node containing a contact. 
{
	next = nullptr;
	string name;
}

bool List::empty()   //bool empty() which returns true if the list is empty
{
	if (head == nullptr) {
		return true;
	}
	else return false;
}

List::List() // Creates a constructor to make an empty list.
{
	head = nullptr;
}

List::~List()
{
	head = nullptr;
	delete head;
}



