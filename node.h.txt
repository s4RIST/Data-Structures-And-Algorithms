#pragma once
using namespace std;
#include <iostream>;
#include "Contact.h";
class Node {  //a contact and a pointer to the next node.
private:
	Contact name;
	Node* nextNode;
public:
	Node(Node* next, Contact name); // a constructor that makes a node containing a contact.
};