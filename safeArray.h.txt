#pragma once

class safeArray {
	friend ostream& operator<<(ostream& lhs, const safeArray& rhs); // fucntion decleration for <<
private:
	int size = 10;
	int* arr;
public:
	safeArray();
	safeArray(int);
	~safeArray();
	int at(int);
	int add(int position, int element);
};
