#pragma once
using namespace std;
#include <iostream>;
#include "Node.h";

class List {
private:
	Node* head = nullptr;   //attribute is: node* head = nullptr.
public:
	
	bool empty();  //have two methods bool empty() which returns true if the list is empty and a constructor to make an empty list.
	List();
	~List();
};